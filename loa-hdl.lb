
def prepare(repo):
	repo.set_name("loa-hdl")
	
	repo.add_modules(repo.glob("modules/*/module.lb"))
	#repo.add_modules("repo1/other.lb")
	
	repo.add_option(
		name="target",
		description="Target architecture for the library.")

	repo.add_numeric_option(
		name="foo",
		default=76,
		description="Something")
